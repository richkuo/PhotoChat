@import "bootstrap";

$grayMediumLight: #eaeaea;
$grayLighter: #eaeaea;
$lightGray: #999;
$blue: #00afff;
$lightText: #abc;

@mixin box_sizing {
   -moz-box-sizing: border-box;
   -webkit-box-sizing: border-box;
   box-sizing: border-box;
}

$navbarBackgroundHighlight: #1e90ff;
$navbarBackground: #000000;

/* left side navigation */

.left-sidenav .icon-chevron-right {
  float: right;
  margin-top: 3px;
  color: $lightText;
}

.left-sidenav a:hover {
  background-color: $blue;
}

.left-sidenav a:hover .icon-chevron-right {
  color: $blue;
}


/* navbar */

#navbarcustomlinks {
  color: $blue;
  &:hover {
    color: white;
    text-decoration: none;
  }
}

.icon-chevron-right {
  float: right;
  vertical-align: middle;
  margin-top: 3px;
  &:hover {
    color: $blue;
  }
}

/* users index */

.users {
  list-style: none;
  margin: 0;
  li {
    overflow: auto;
    padding: 10px 0;
    border-top: 1px solid $grayLighter;
    &:last-child {
      border-bottom: 1px solid $grayLighter;
    }
  }
}

/* microposts */

.microposts {
  list-style: none;
  margin: 10px 0 0 0;

  li {
    padding: 10px 0;
    border-top: 1px solid #e8e8e8;
  }
}

.content {
  display: block;
}

/*
.timestamp {
  color: $lightText;
}
*/

.gravatar {
  float: left;
  margin-right: 10px;
}

aside {
  textarea {
    height: 100px;
    margin-bottom: 5px;
  }
}

/* header */

@-webkit-keyframes pulsate {
	0% {
		color: $lightText;
		transition: 500ms linear 1s;
	}
	100% {
		color: $blue;
		transition: 500ms linear 1s;
	}
}

@-moz-keyframes pulsate {
	0% {
		color: $lightText;
		transition: 500ms linear 1s;
	}
	100% {
		color: $blue;
		transition: 500ms linear 1s;
	}
}

@keyframes pulsate {
	0% {
		color: $lightText;
		transition: 500ms linear 1s;
	}
	100% {
		color: $blue;
		transition: 500ms linear 1s;
	}
}

#logo {
  float: left;
  margin-right: 10px;
  font-size: 1.7em;
  color: #abc;
  padding-top: 9px;
  line-height: 1;
  font-weight: bold;
  &:hover {
    color: $blue;
    text-decoration: none;
    -webkit-animation-name: pulsate;
    -webkit-animation-duration: 1.5s;
    -webkit-animation-iteration-count: infinite;
    -webkit-animation-direction: alternate;
    -webkit-animation-timing-function: ease-in-out;

    -moz-animation-name: pulsate;
    -moz-animation-duration: 1.5s;
    -moz-animation-iteration-count: infinite;
    -moz-animation-direction: alternate;
    -moz-animation-timing-function: ease-in-out;

    animation-name: pulsate;
    animation-duration: 1.5s;
    animation-iteration-count: infinite;
    animation-direction: alternate;
    animation-timing-function: ease-in-out;
  }
}


/* footer */

footer {
  margin-top: 45px;
  padding-top: 5px;
  border-top: 1px solid $blue;
  color: $lightText;
  a {
    color: $lightText;
    &:hover {
      color: $blue;
      text-decoration: none;
    }
  }
  small {
    float: left;
  }
  ul {
    float: right;
    list-style: none;
    li {
      float: left;
      margin-left: 10px;
    }
  }
}


/* universal */

html {
  overflow-y: scroll;
}

body {
  padding-top: 65px;
}

section {
  overflow: auto;
}

textarea {
  resize: vertical;
}

.center {
  text-align: center;
  h1 {
    margin_bottom: 10px;
  }
}

a, a:hover {
  text-shadow: none;
  text-decoration: none;
  -webkit-transition: all linear .7s;
  -moz-transition: all linear .7s;
  transition: all linear .7s;
}
a:hover {
  color: $blue;
  text-shadow: -2px 2px 7px $lightText;
  text-decoration: underline;
}

/* typography */

h1, h2, h3, h4, h5 {
  line-height: 1;
}

#thumb {
  padding: 0px 1px;
  line-height: 7.5;
}

h2 {
  color: #999;
}

h3 {
  color: $lightText;
}

/* miscellaneous */

.debug_dump {
   clear: both;
   float: left;
   width: 100%;
   margin-top: 45px;
   @include box_sizing;
}

/* sidebar */

.stats {
  overflow: auto;
  a {
    float: left;
    padding: 0 10px;
    border-left: 1px solid $grayLighter;
    color: gray;
    &:first-child {
      padding-left: 0;
      border: 0;
    }
    &:hover {
      text-decoration: none;
      color: $blue;
    }
  }
  strong {
    display: block;
  }
}

.user_avatars {
  overflow: auto;
  margin-top: 10px;
  .gravatar {
    margin: 1px 1px;
  }
}

aside {
   section {
      padding: 10px 0;
      border-top: 1px solid $grayLighter;
      &:first-child {
         border: 0;
         padding-top: 0;
      }
      span {
         display: block;
         margin-bottom: 3px;
         line-height: 1;
      }
      h1 {
         font-size: 1.6em;
         text-align: left;
         letter-spacing: -1px;
         margin-bottom: 3px;
      }
   }
}

.gravatar {
   float: left;
   margin-right: 10px;
}

/* forms */

input, textarea, input[type="text"], input[type="password"], select, .uneditable-input {
   border: 2px solid $lightText;
   width: 100%;
   padding: 10px;
   height: 40px;
   font-size: 20px;
   margin-bottom: 15px;
   @include box_sizing;
}

#error_explanation {
  color: #f00;
  ul {
    list-style: none;
    margin: 0 0 18px 0;
  }
}

.field_with_errors {
  @extend .control-group;
  @extend .error;
}
