<div class="left-sidenav">
<div class="btn-group btn-group-vertical btn-block">

<% if host?(@event) %>

  <a href="<%= event_uploaders_path(@event) %>"><button class="btn"><i class="icon-chevron-right"></i>Invite Your Friends!</button></a>

<% end %>


<% if current_user.reverse_invitations.find_by_event_id(params[:event_id]) || current_user.reverse_invitations.find_by_event_id(params[:id]) || host?(@event) %>

  <a href="<%= new_event_picture_path(@event) %>"><button class="btn"><i class="icon-chevron-right"></i>Upload Pictures</button></a>

  <a href="<%= event_path(@event) %>"><button class="btn"><i class="icon-chevron-right"></i>View Event</button></a>

<% end %>

<a href="<%= user_path(current_user) %>"><button class="btn"><i class="icon-chevron-right"></i>Other Events</button></a>

</div>
</div>
