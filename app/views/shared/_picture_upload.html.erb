<%= form_for @picture, :html => {:multipart => true} do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <p>
    <%= f.file_field :image %>
  </p>
  <br>
  <p><%= f.submit %></p>
<% end %>
