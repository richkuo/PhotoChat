<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "200 characters max" %>
  </div>
  <%= f.submit "Leave feedback", class: "btn btn-large btn-primary" %>
<% end %>
