<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <%= render 'shared/user_info' %>
    </section>
      
      <%= render 'shared/host_event' %>
    
      <%= render 'shared/stats' %>
  </aside>
  <div class="span8">

<% unless current_user?(@user) %>

    <%= render 'uploader_form' if host?(current_event) %>

<% else %>

You cannot invite yourself!

<% end %>
  </div>
</div>
